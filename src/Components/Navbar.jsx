function Navbar(){

    return(
        <nav>
            <button>Initialize Array</button>
            <input type='range'/>
            <button>Randomize Array</button>
            <button>Start Sort</button>
        </nav>
    );
}

export default Navbar;